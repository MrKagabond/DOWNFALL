package DOWNFALL;
//This class is an enumeration for direction

//Primarily used by the projectile classes to denote which way that should move

public enum Direction_System {
	Up(), Down(), Left(), Left_Up(), Left_Down(), Right();
}
